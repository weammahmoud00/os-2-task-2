on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: docker login --username weammahmoud00 --password dckr_pat_S9ji8OFwEORXRlRcy5OodwQI4Ms
